#!/bin/bash
# Cynovela を起動する入口 (・決定 29-3)
#   ダブルクリックするとターミナルが開き、この中でガイドを読みながら進めます。
#   起動の理屈は ./launch.sh (包み) と tools/launch-body.sh (本体) に在り、
#   このファイルは同じフォルダへ移動して包みを呼ぶだけです。
#   引数なし＝本番 (中身が空) / --demo＝同梱のダミー資料を使うデモ (初回起動時に自動で取り込まれる)。
#   ダブルクリックでは引数が付かないため本番で立ち上がります。
#   端末から引数を付けて叩いたときは、そのまま ./launch.sh へ渡します (§3-D)。
#   止めるときは Cynovela-stop.command をダブルクリックしてください。
set -u
cd "$(dirname "$0")"
exec ./launch.sh "$@"

#!/bin/bash
# Cynovela を起動する入口 (DD-CYN-0070・決定 29-3)
#   ダブルクリックするとターミナルが開き、この中で案内を読みながら進めます。
#   起動の理屈は ./launch.sh (包み) と tools/launch-body.sh (本体) に在り、
#   このファイルは同じフォルダへ移動して包みを呼ぶだけです。
#   引数なし＝本番 (中身が空) / --demo＝同梱のダミー資料が載った状態 (決定 29-2)。
#   ダブルクリックでは引数が付かないため本番で立ち上がります。
#   端末から引数を付けて叩いたときは、そのまま ./launch.sh へ渡します (DD-CYN-0095 §3-D)。
#   止めるときは Cynovela-stop.command をダブルクリックしてください。
set -u
cd "$(dirname "$0")"
exec ./launch.sh "$@"

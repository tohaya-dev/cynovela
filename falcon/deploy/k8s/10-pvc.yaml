apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cynovela-data
  namespace: cynovela
  labels:
    app: cynovela
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 8Gi

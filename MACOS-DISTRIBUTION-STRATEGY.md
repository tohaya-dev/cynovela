# Cynovela macOS Distribution Strategy

Status: **Decision / investigation record**  
Date: **2026-08-29**  
Scope: Apple Silicon macOS public OSS distribution

## 1. Decision summary

Cynovela remains **one product, one codebase, one version, and one GitHub Release**.

A release may publish **two distribution artifacts for the same Cynovela version**:

```text
Cynovela <version>
├── Cynovela-<version>-macos-arm64.pkg
└── Cynovela-<version>-macos-arm64-portable.tar.gz
```

These are not separate editions, products, or version lines. They are two delivery formats of the same release.

- **PKG artifact**: primary macOS installation path, targeting installation of a normal `Cynovela.app` launcher/application experience.
- **Portable artifact**: installer-free fallback for managed/company Macs or environments where `.pkg` installation is restricted or undesirable.

Both artifacts are intended to remain public OSS release assets.

## 2. Why two artifacts are retained

The current self-contained conda-pack-based distribution has already been proven to run on at least one managed company Mac. A future `.pkg` may also work there, but MDM and endpoint policies are organization-specific and can restrict Installer packages independently of whether a portable archive currently runs.

Therefore the operational policy is:

```text
Try PKG
  ├── works -> use PKG
  └── blocked by the managed environment -> use/test Portable
```

Portable is a practical fallback, **not a guarantee** that every managed Mac will allow it. A future MDM policy could block either form.

## 3. Current package terminology

The existing artifact historically called a "Conda Package" is **not a formal Conda package** (`.conda` or `.tar.bz2`).

Its actual model is a portable archive containing a `conda-pack` environment. To avoid confusion with real Conda packaging, future user-facing naming should use **Portable** rather than "Conda Package" for this distribution form.

## 4. Findings from the current public artifact

The inspected public package contains developer/build-machine absolute paths, including paths under:

```text
/Users/toshi-mac-air/...
```

The large majority were found in Python bytecode (`.pyc`), with additional occurrences in two Mach-O Python extension modules.

Two relevant `.pyc` path forms were observed:

### Form A — original Conda environment prefix

```text
/Users/toshi-mac-air/miniforge3/envs/cynovela-dist/...
```

### Form B — post-pack extraction/workspace prefix

```text
/Users/toshi-mac-air/cynovela-work-.../chewie/.condapack-cynovela/...
```

Form B strongly indicates that Python was executed after the original conda-pack archive was expanded into a work directory, generating fresh bytecode before the final release archive was created.

A general statement that `conda-unpack` can never affect any `.pyc` would be too broad. However, a `.pyc` created **after** the original pack operation cannot have been part of that pack operation's relocation records. The Form B files therefore cannot be repaired by the original conda-pack relocation metadata.

## 5. Python bytecode policy

The inspected package contained no known orphan `.pyc` files in the audit: the corresponding `.py` sources were present.

For release artifacts, the preferred policy is therefore:

- remove `__pycache__` directories and `.pyc` files before final packaging unless there is a demonstrated reason to retain them;
- do not execute the bundled environment in a staging directory and then re-archive newly generated bytecode;
- run startup, critical-import, and E2E tests after bytecode cleanup;
- if bytecode is intentionally precompiled in the future, use a deterministic path policy rather than recording a developer workspace path.

## 6. Mach-O findings and correction

The two inspected `.so` files contain a real Mach-O load command:

```text
LC_RPATH /Users/toshi-mac-air/miniforge3/envs/cynovela-dist/lib
```

Their inspected `LC_LOAD_DYLIB` entries point to system libraries such as:

```text
/usr/lib/libz.1.dylib
/usr/lib/libSystem.B.dylib
```

No relevant `@rpath/...` dependency was observed in those files. The stale developer `LC_RPATH` therefore appears unused in the current binaries, which is consistent with the artifact running on another Mac despite the stale path.

This path should still be removed from a public artifact because it is unnecessary developer-machine metadata.

The appropriate repair is **not manual byte patching**. Use the macOS Mach-O tooling, for example:

```bash
install_name_tool -delete_rpath "/Users/toshi-mac-air/miniforge3/envs/cynovela-dist/lib" <binary>
```

Then re-inspect the Mach-O load commands and rerun import/startup/E2E tests. Any future code signing must happen **after** Mach-O modification.

## 7. Target PKG architecture

The preferred PKG path to validate is:

```text
Cynovela source
  -> formal Conda package(s)
  -> Constructor
  -> macOS arm64 .pkg
  -> installation
  -> Cynovela.app
```

The reason for evaluating this path is not that Conda itself is a problem. The problem in the current model is moving an already-installed development environment snapshot as the public artifact.

A formal Conda package plus target-side installation can use Conda's package/install model for dependency management, target prefixes, and native package handling rather than relying on relocation of a developer environment snapshot.

The final `.app` may be a thin macOS application/launcher that starts the installed Cynovela runtime. `menuinst` or another minimal launcher mechanism should be evaluated against the actual repository and current Constructor behavior; no implementation should be selected purely by assumption.

## 8. Portable architecture

Portable remains a supported delivery form of the same release, primarily because it provides an installer-free path and already has a managed-Mac usage precedent.

The current conda-pack implementation must not simply be preserved unchanged. At minimum the Portable build must be sanitized and tested:

- remove unnecessary `.pyc` / `__pycache__`;
- remove stale developer `LC_RPATH` values;
- use conda-pack relocation correctly if conda-pack remains the implementation;
- avoid unpack -> execute -> re-tar workflows that regenerate build/workspace paths;
- validate critical imports, startup, and main E2E workflows;
- test extraction into multiple destination paths;
- perform final artifact scanning after the archive is completely built.

Whether conda-pack remains the long-term Portable implementation or is later replaced by pixi-pack or another reproducible mechanism is a separate implementation decision. It does not change the two-artifact release policy.

## 9. Final artifact release gate

Source-tree scans are insufficient. The actual release artifacts must be inspected.

Before publication, both PKG contents and Portable contents should be expanded/inspected and checked for unintended local information, including:

```text
/Users/
toshi-mac-air
local workspace names
build directories
private host names
internal file names
credentials
tokens
API keys
```

The scan must include binary files where practical, not only text files.

Release validation should also include:

- clean/reproducible build input;
- package/build success;
- artifact extraction/inspection success;
- developer-path/privacy scan;
- secret scan;
- Mach-O dependency/RPATH inspection where relevant;
- critical import tests;
- application startup test;
- representative E2E workflow test;
- checksum generation;
- version consistency across artifacts.

Published release assets should be treated as immutable. If an artifact changes, publish it under a new release/version rather than silently replacing an existing asset.

## 10. Apple signing and notarization

Apple Developer Program membership, Developer ID signing, and notarization are **not prerequisites for evaluating or creating the Constructor/PKG architecture**.

They are optional distribution-hardening measures that reduce Gatekeeper friction and provide Apple Developer ID/notarization assurances. They also introduce recurring cost and release-operation overhead.

For Cynovela's current niche, technical-validation OSS audience, signing/notarization is **not a go/no-go condition** for the PKG design. It can be added later if the project reaches a point where Gatekeeper friction or user expectations justify the cost and operational burden.

The PKG and Portable artifacts may therefore be technically validated and publicly released without making Apple Developer Program enrollment part of the immediate packaging decision.

## 11. Managed/company Mac policy

Do not design speculative MDM workarounds into Cynovela before there is evidence they are required.

The test policy is empirical:

1. test the PKG on normal personal Macs;
2. test the same PKG on the currently managed company Mac;
3. if it works, use the PKG normally;
4. if the managed environment specifically blocks the PKG, use/test the Portable artifact;
5. document the observed behavior rather than claiming universal managed-Mac compatibility.

The existing success of the current portable package is an observed compatibility result for the current policy, not a universal guarantee.

## 12. What is unified and what is not

Unified:

- product identity;
- source tree;
- application version;
- GitHub Release;
- release notes;
- functional behavior;
- release-quality gates.

Not forced into one file format:

- delivery mechanism.

Therefore **PKG + Portable does not mean two products or two editions**. It means two artifacts of the same OSS release for different installation constraints.

## 13. Relationship to Kyber and CynoCarina

The release-engineering principles discovered here should also be considered for Kyber and CynoCarina:

- scan final artifacts, not just source;
- do not leak developer/build-machine paths;
- keep published assets immutable;
- validate startup and critical workflows;
- distinguish product/version identity from delivery format;
- support more than one delivery artifact only where the target environment justifies it.

The exact packaging implementation does **not** need to be identical across projects. Constructor is specifically attractive when the application's actual runtime/dependency architecture makes formal Conda packaging appropriate.

## 14. Next engineering question

The next step is implementation-level validation, not further abstract packaging debate:

1. determine the minimum work required to package current Cynovela as a formal Conda package;
2. build a Constructor-generated Apple Silicon `.pkg`;
3. establish the simplest maintainable `Cynovela.app` launcher integration;
4. sanitize the Portable pipeline using the findings above;
5. run both artifacts through the same final-artifact hygiene and E2E gates;
6. test the PKG on both personal and managed Mac environments and record the actual result.

No release version number is decided by this document.
